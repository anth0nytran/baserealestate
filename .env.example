# Resend API key for the lead form (api/send.ts).
RESEND_API_KEY=

# Where new leads are delivered. Sam's inbox.
LEAD_TO_EMAIL=sam.elsherif@gmail.com

# Optional comma-separated BCC list for lead notifications.
LEADS_BCC_EMAIL=

# TODO(client): domain not chosen yet. Must match SITE_URL in
# src/config/site.ts and the @id values in index.html.
SITE_URL=https://www.baserealestate.com

# Optional. Injected as <meta name="google-site-verification"> at build time.
GOOGLE_SITE_VERIFICATION=
