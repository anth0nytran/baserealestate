# Normalise line endings so the repo is identical on Windows, macOS, and the
# Linux builders Vercel runs on.
* text=auto eol=lf

# Binary assets must never be line-ending converted.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
